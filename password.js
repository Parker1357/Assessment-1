const string = "Welcome to the password validator tool! We need a password to validate"
var passwordLength = 12
if (passwordLength > 10) {
    console.log ("Success")
} else (
    console.log ("failure")
)
